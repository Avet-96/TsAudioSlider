import React from "react";
import {SliderBlock, style} from '../footer-styled/slider'

const PlayerSlider = () => {

    return (
    <SliderBlock>
        <span style={{...style, width: 70 + '%'}}/>
    </SliderBlock>
    )
}

export default PlayerSlider
